# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Minimal test project with a single Python script (`hello.py`) that prints "Hello, World!". No dependencies, no build system, no test framework.

## Commands

```bash
# Run the script
python hello.py
```

No linting, formatting, or test infrastructure is configured.
